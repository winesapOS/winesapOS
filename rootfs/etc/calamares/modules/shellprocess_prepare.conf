---
# Repair file systems that would otherwise make the partitioning step fail. This runs outside of the
# target system because it inspects the storage devices before they have been partitioned.
dontChroot: true

script:
    - command: "/bin/bash /usr/bin/winesapos-install-prepare.sh"
      timeout: 3600
